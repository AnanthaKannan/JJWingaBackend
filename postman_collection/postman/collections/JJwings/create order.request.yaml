$kind: http-request
url: "{{url}}/admin/order"
method: POST
headers:
  x-access-token: "{{token}}"
auth:
  type: apikey
  credentials:
    key: x-api-key
    value: "{{api_key}}"
settings: {}
order: 13000
