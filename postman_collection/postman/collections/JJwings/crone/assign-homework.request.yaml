$kind: http-request
url: "{{url}}/crone/assign-homeworks"
method: POST
headers:
  x-access-token: "{{token}}"
auth:
  type: apikey
  credentials:
    key: x-api-key
    value: "{{api_key}}"
settings: {}
order: 3000
